% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sniff_csv.R
\name{sniff_csv}
\alias{sniff_csv}
\title{Report how a CSV file would be read}
\usage{
sniff_csv(file = NULL, delimiter = ",", na = c("", "NA"), text = NULL)
}
\arguments{
\item{file}{Path to a local file, as a single string. \code{~} is expanded.
Supply this or \code{text}, not both.}

\item{delimiter}{The field delimiter, as a single ASCII character.
Defaults to \code{","}. Not detected --- \code{zucsv} never guesses the delimiter.}

\item{na}{Character vector of cell values to read as missing, or \code{NULL} to
disable missing-value matching. Affects the inferred types, so pass here
whatever you will pass to \code{\link[=read_csv]{read_csv()}}.}

\item{text}{The CSV itself, as a character vector, instead of a path, with
the same meaning as in \code{\link[=read_csv]{read_csv()}}.}
}
\value{
A list with five elements:
\describe{
\item{\code{header}}{\code{TRUE} if the first record is names, \code{FALSE} if it is
data, by the rule documented under \code{header} in \code{\link[=read_csv]{read_csv()}}.}
\item{\code{ncol}}{Number of columns.}
\item{\code{nrow}}{Number of data rows --- exact, not an estimate, because
the pass counts every record.}
\item{\code{col_names}}{The names the file would produce: the first record
when \code{header} is \code{TRUE}, otherwise \code{V1}, \code{V2}, ...}
\item{\code{col_types}}{Inferred type of each column, suitable for passing
straight back as \code{col_types}.}
}
An empty file gives \code{header = FALSE}, zero columns and zero rows.
}
\description{
Runs the first of \code{\link[=read_csv]{read_csv()}}'s two passes and returns what it learned ---
the header verdict, the shape, the column names and the inferred column
types --- without building any columns. Use it when you want to see the
decisions before committing to them, or to pin them down so a later run
cannot drift:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{s <- sniff_csv(path)
read_csv(path, header = s$header, col_types = s$col_types)
}\if{html}{\out{</div>}}

That second call reads the file strictly: nothing about it is inferred, so
it gives the same answer next month even if the data changes shape.

Sniffing reads the whole file and applies exactly the validation
\code{\link[=read_csv]{read_csv()}} applies, so a ragged row, an embedded NUL or invalid UTF-8 is
an error here too. If \code{sniff_csv()} succeeds, \code{read_csv()} on the same file
will not fail on content.

It costs one pass rather than two, but that pass is the expensive one: do
not call it before every read as a matter of course. Its purpose is to let
you \emph{stop} inferring, not to infer twice.
}
\examples{
path <- tempfile(fileext = ".csv")
writeLines(c("id,name,score", "1,Ada,9.5", "2,Grace,8"), path)

sniff_csv(path)

# Pin the decisions, then read without inferring anything
s <- sniff_csv(path)
read_csv(path, header = s$header, col_types = s$col_types)

unlink(path)
}
\seealso{
\code{\link[=read_csv]{read_csv()}}
}
